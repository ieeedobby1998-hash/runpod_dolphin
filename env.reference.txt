# Env vars for the RunPod Serverless endpoint.
# These match the image defaults — you only NEED to set ones you want to change.

MODEL_REPO=bartowski/dolphin-2.9.2-mixtral-8x22b-GGUF
MODEL_FILE=dolphin-2.9.2-mixtral-8x22b-Q5_K_M.gguf/dolphin-2.9.2-mixtral-8x22b-Q5_K_M-00001-of-00003.gguf
MODEL_ALLOW_PATTERNS=dolphin-2.9.2-mixtral-8x22b-Q5_K_M.gguf/*
MODEL_DIR=/runpod-volume/models

N_GPU_LAYERS=-1
CTX_SIZE=8192
PARALLEL=1
LLAMA_PORT=8080

# Only if using 2 GPUs and you want an explicit weight split (e.g. two equal GPUs):
# TENSOR_SPLIT=1,1

# Only for gated/private HF repos (bartowski's is public, so leave unset):
# HF_TOKEN=hf_xxx

# Increase if 99GB download + load ever exceeds 30 min on a slow first boot:
# BOOT_LIMIT_SECONDS=1800
